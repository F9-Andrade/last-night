{
  "name": "last-night",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --experimental-strip-types tests/simulation.test.ts && node --experimental-strip-types tests/voxel.test.ts && node --experimental-strip-types tests/survival.test.ts && node --experimental-strip-types tests/combat.test.ts && node --experimental-strip-types tests/variety.test.ts && node --experimental-strip-types tests/stamina.test.ts && node --experimental-strip-types tests/city.test.ts",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
